management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  health:
    readinessState:
      enabled: true
    livenessState:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
spring:
  profiles:
    active: ${ATLAS_APP_ENVIRONMENT:local}
