management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  health:
    readinessState:
      enabled: true
    livenessState:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
minio:
  endpoint: http://localhost:9000
  accessKey: user
  secretKey: password
  bucket: nvdb-tnits-local-data-01
